export enum ListFilterTypeEnum {
  select = 'select',
  text = 'text',
  date = 'date',
  number = 'number',
  multiSelect = 'multiSelect',
  autocomplete = 'autocomplete',
  checkbox = 'checkbox',
}
