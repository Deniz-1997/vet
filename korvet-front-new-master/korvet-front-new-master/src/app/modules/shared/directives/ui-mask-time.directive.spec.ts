import { UiMaskTimeDirective } from './ui-mask-time.directive';

describe('UiMaskTimeDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiMaskTimeDirective();
    // expect(directive).toBeTruthy();
  });
});
