import { UiDatepickerDirective } from './ui-datepicker.directive';

describe('UiDatepickerDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiDatepickerDirective();
    // expect(directive).toBeTruthy();
  });
});
