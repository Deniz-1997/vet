import { UiMaskNumberDirective } from './ui-mask-number.directive';

describe('UiMaskNumberDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiMaskNumberDirective();
    // expect(directive).toBeTruthy();
  });
});
