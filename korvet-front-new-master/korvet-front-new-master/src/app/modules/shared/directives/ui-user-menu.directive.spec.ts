import { UiUserMenuDirective } from './ui-user-menu.directive';

describe('UiUserMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiUserMenuDirective();
    // expect(directive).toBeTruthy();
  });
});
