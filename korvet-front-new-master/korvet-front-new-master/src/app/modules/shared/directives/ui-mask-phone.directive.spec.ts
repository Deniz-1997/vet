import { UiMaskPhoneDirective } from './ui-mask-phone.directive';

describe('UiMaskPhoneDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiMaskPhoneDirective();
    // expect(directive).toBeTruthy();
  });
});
