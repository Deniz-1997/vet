import { UiSelectFieldDirective } from './ui-select-field.directive';

describe('UiSelectFieldDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiSelectFieldDirective();
    // expect(directive).toBeTruthy();
  });
});
