import { MenuColumnSliderDirective } from './menu-column-slider.directive';

describe('MenuColumnSliderDirective', () => {
  it('should create an instance', () => {
    // const directive = new MenuColumnSliderDirective();
    // expect(directive).toBeTruthy();
  });
});
