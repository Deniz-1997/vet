import { UiSelectDirective } from './ui-select.directive';

describe('UiSelectDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiSelectDirective();
    // expect(directive).toBeTruthy();
  });
});
