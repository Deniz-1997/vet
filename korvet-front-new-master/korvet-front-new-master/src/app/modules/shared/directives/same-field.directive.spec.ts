import { SameFieldDirective } from './same-field.directive';

describe('SameFieldDirective', () => {
  it('should create an instance', () => {
    // const directive = new SameFieldDirective();
    // expect(directive).toBeTruthy();
  });
});
