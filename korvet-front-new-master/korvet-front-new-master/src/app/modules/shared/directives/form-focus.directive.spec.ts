import { FormFocusDirective } from './form-focus.directive';

describe('FormFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFocusDirective();
    expect(directive).toBeTruthy();
  });
});
