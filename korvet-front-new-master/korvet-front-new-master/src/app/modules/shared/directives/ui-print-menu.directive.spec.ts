import { UiPrintMenuDirective } from './ui-print-menu.directive';

describe('UiPrintMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiUserMenuDirective();
    // expect(directive).toBeTruthy();
  });
});
