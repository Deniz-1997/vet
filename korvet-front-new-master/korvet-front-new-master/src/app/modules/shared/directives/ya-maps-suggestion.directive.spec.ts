import { YaMapsSuggestionDirective } from './ya-maps-suggestion.directive';

describe('YaMapsSuggestionDirective', () => {
  it('should create an instance', () => {
    // const directive = new YaMapsSuggestionDirective();
    // expect(directive).toBeTruthy();
  });
});
