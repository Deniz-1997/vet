import { UiNotificationToggleDirective } from './ui-notification-toggle.directive';

describe('UiNotificationToggleDirective', () => {
  it('should create an instance', () => {
    // const directive = new UiNotificationToggleDirective();
    // expect(directive).toBeTruthy();
  });
});
