import {PetMainOwnerPipe} from './pet-main-owner.pipe';

describe('PetMainOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new PetMainOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});
