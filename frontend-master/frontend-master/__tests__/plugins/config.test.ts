// import axios from 'axios';
// import plugin from '@/plugins/config';
// import { checkPluginRegistration } from './__utils__/checkPluginRegistration';

describe.skip('config plugin', () => {
  test('registrates plugin', async () => {
    // const mock = jest.spyOn(axios, 'get').mockImplementationOnce(() => Promise.resolve({ data: { foo: 'bar' } }));
    // const ctx = await checkPluginRegistration(() => plugin);
    // expect(ctx).toHaveProperty('$config');
    // expect(ctx.$config.foo).toBe('bar');
    // expect(ctx.$config.isUserActive).toBe(false);
    // expect(ctx.$config.isDev).toBe(false);
    // expect(mock).toBeCalledWith(`/configs/${process.env.NODE_ENV}.json`);
  });
});
