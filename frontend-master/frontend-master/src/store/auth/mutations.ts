export default {
  setUserInfo: (state, value) => {
    state.user = value;
  },
  setMatrix(state, matrix) {
    state.matrix = matrix;
  },
  setTitles(state, titles) {
    state.titles = titles;
  },
};
